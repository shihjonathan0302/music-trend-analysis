# Python virtual environment (recreate from requirements.txt instead)
.venv/
__pycache__/
.ipynb_checkpoints/

# Raw data is large (~500MB); download from Kaggle per the README instead of committing
data/raw/

# Derived artifacts — regenerate by running notebooks 02 (cleaning) and 03 (load)
data/processed/
db/

# macOS
.DS_Store
